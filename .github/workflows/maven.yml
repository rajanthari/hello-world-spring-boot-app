name: Hello World App
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
            echo "Hello World!"
            echo "Hello Rajesh!"
      - name: Print goodbye
        run: echo "Good bye!"
      
